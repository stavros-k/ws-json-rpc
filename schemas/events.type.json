{
    "$schema": "../type-schema.json",
    "SomeEvent": {
        "kind": "object",
        "description": "Result for the SomeEvent method",
        "fields": [
            {
                "name": "id",
                "type": "string",
                "format": "uuid",
                "description": "The unique identifier for the result"
            }
        ]
    },
    "EventDataMap": {
        "kind": "map",
        "description": "A map of event data indexed by event ID",
        "keyType": "string",
        "valueType": {
            "$ref": "SomeEvent"
        }
    }
}
