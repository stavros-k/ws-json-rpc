# TODO

- [ ] Remove the type generation
- [ ] Add a `types.ts` in the `ws-client`, with the "hard-coded" types
- [ ] Disallow the `subscribe` and `unsubscribe` methods from the `call` method
- [ ] Reduce use of abstract generics in `ws-client`, to improve DX.
- [ ] Update docs after the removal of type generation
- [ ] Add a `ws-client` example to the examples folder
- [ ] Create a go-ws-client package (for testing purposes)
- [ ] Create a csharp-ws-client package and it's types
